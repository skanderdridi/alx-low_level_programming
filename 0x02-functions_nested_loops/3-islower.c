#include "main.h"

/**
 * _islower - check if character is lowercase
 * @c: character
 *
 *
 * Return: 1 lowercase 0 otherwise
 **/
int main(void)
{
	int c;
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
